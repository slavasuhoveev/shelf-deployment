apiVersion: apps/v1
kind: StatefulSet

metadata:
  name: {{ .Values.shelfAuthDb.name }}
  namespace: {{ .Values.global.namespace }}
  labels:
    app.kubernetes.io/name: {{ .Values.shelfAuthDb.name }}
    app.kubernetes.io/component: database

spec:
  serviceName: {{ .Values.shelfAuthDb.name }}
  replicas: 1

  selector:
    matchLabels:
      app.kubernetes.io/name: {{ .Values.shelfAuthDb.name }}

  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ .Values.shelfAuthDb.name }}
        app.kubernetes.io/component: database

    spec:
      containers:
        - name: {{ .Values.shelfAuthDb.name }}
          image: "{{ .Values.shelfAuthDb.image.repository }}:{{ .Values.shelfAuthDb.image.tag }}"
          imagePullPolicy: {{ .Values.shelfAuthDb.image.pullPolicy }}

          ports:
            - containerPort: {{ .Values.shelfAuthDb.service.port }}

          env:
            {{- range $key, $value := .Values.shelfAuthDb.env }}
            - name: {{ $key }}
              value: {{ $value | quote }}
            {{- end }}

          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data

          readinessProbe:
            exec:
              command:
                - sh
                - -c
                - pg_isready -U {{ .Values.shelfAuthDb.env.POSTGRES_USER }} -d {{ .Values.shelfAuthDb.env.POSTGRES_DB }}
            initialDelaySeconds: 5
            periodSeconds: 10

          livenessProbe:
            exec:
              command:
                - sh
                - -c
                - pg_isready -U {{ .Values.shelfAuthDb.env.POSTGRES_USER }} -d {{ .Values.shelfAuthDb.env.POSTGRES_DB }}
            initialDelaySeconds: 15
            periodSeconds: 20

  volumeClaimTemplates:
    - metadata:
        name: data

      spec:
        accessModes:
          - ReadWriteOnce

        resources:
          requests:
            storage: {{ .Values.shelfAuthDb.storage.size }}
